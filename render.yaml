services:
  - type: web
    name: slovak-speech-sim
    env: python
    region: frankfurt       # choose a region close to you
    plan: free              # or “starter” / “standard” as you like
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    autoDeploy: true
