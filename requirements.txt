# Use the PyTorch CPU index so pip never pulls CUDA wheels
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.2.2+cpu         # ~150 MB wheel instead of 865 MB
torchaudio==2.2.2+cpu    # (smaller too, optional if you use torchaudio)

# the rest of your deps
Flask
Flask-SQLAlchemy
transformers
librosa
numpy
scipy
scikit-learn
python-Levenshtein
gunicorn
matplotlib

